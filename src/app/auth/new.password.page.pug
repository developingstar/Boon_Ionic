ion-content(padding)
  form(#newPasswordForm="ngForm", (ngSubmit)="createNewPassword()", autocomplete="off")
    ion-row
      ion-col
        h1 Create new password
    ion-row
      ion-col
        ion-label(stacked) Code
        ion-input(name="code", type="text", placeholder="Please enter the code sent to the provided email address.", [(ngModel)]="codeModel", required, #code)
    ion-row
      ion-col
        ion-label(stacked) New password
        ion-input(name="newPassword", type="password", placeholder="Please input new password", [(ngModel)]="newPasswordModel", required, #newPassword)
    ion-row
      ion-col
        ion-label(stacked) Confirm new password
        ion-input(name="confirmPassword", type="password", placeholder="Please input confirm password", [(ngModel)]="confirmPasswordModel", required, #confirmPassword)
    ion-row
      ion-col
        hr
    ion-row
      ion-col(col-4, offset-8)
        button(ion-button, full, type="submit", [disabled]="!newPasswordForm.form.valid")
          | Submit Code
