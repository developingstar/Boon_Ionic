nav-content
  nav-icons(right, currentPage="TextTemplatesPage")

ion-content(padding)
  .templates-header
    h2 Text Message Templates
    button.new-template(ion-button, (click)="newTemplate()") New Template

  ion-grid.table
    ion-row.header
      ion-col Name
      ion-col Default sender
      ion-col.action
    ion-row.template-row(*ngFor="let template of (templates | async)", (click)="show(template)")
      ion-col {{template.name}}
      ion-col {{template.defaultSender}}
      ion-col.action
        div.dot
        div.dot
        div.dot
